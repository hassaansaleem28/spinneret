# Bright Data API token — Account Settings -> API Tokens
# Apply the hackathon promo code "wemakedevs" (lowercase) in the billing section
# for the extra $50 of credits on top of the 5,000 free monthly credits.
BRIGHTDATA_API_KEY=your_token_here

# Optional: override where the flight recorder is stored.
# SPINNERET_DB=./data/spinneret.db
